[build]
jobs = 1

# Run tests sequentially to avoid shared-state conflicts (env vars, on-disk
# databases, etc.). Cargo has no `[test]` section, so we set the libtest env
# var that controls the default thread count for `cargo test`.
[env]
RUST_TEST_THREADS = "1"
