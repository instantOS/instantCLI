implement the reversemouse setting from the original instantmouse in `ins
settings` and also persist and restore it. For now only X11 with others doing
nothing (but notify the user that it's not supported).

Later, look into how to do it on Sway
