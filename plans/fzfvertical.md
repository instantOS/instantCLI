
Extend the builder pattern for fzf to support horizontal and vertical layout
(vertical being the default).

Also, long messages with the message feature could benefit from wrapping. Look
up if fzf can do that or if we need to manually wrap lines before passing them
to fzf

