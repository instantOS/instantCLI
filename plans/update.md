I want to introduce an `ins update` command. 
It should be pretty simple. First, it ensures `topgrade` is installed (there are
utils for this already there, maybe some of them need to be extractede, maybe
not), then it runs it. 

Afterwards it runs `ins dot update` to update the dotfiles






