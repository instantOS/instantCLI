Similarly to the mouse dpi setting, brightness should also get an entry in the
settings app. In its description it should also hint that the same setting can
be accessed via instantASSIST (Super+A b)

The setting should be persisted and restoerd just like the dpi setting. 
